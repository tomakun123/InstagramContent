# Story -> narration. edge-tts also supplies word-level timings via
# WordBoundary events, which is why no speech-recognition step is needed.
edge-tts
python-dotenv

# NOTE: ffmpeg must be installed separately and available on PATH.
#       It does the music mix, the crop/scale, the subtitle burn-in (libass)
#       and the NVENC encode.
#       https://www.ffmpeg.org/download.html#build-windows

# Watcher
watchdog

# --- optional ---------------------------------------------------------------
# Only needed for `--renderer moviepy`, the legacy render path kept for visual
# comparison. The default ffmpeg renderer does not use it.
# moviepy
# pillow
